# 第 1 章 · 1.7 本章总结与知识整合
### Chapter Summary and Knowledge Integration

上一节：[1.6.Numerical stability.md](1.6.Numerical stability.md)

---

## 1.7 本章总结与知识整合

## 开篇：这一章解决了什么问题

线性代数是数据科学的「通用语言」——学完这章，你应该能用矩阵和向量的视角看待现实世界的数据问题了。

当你再看到一张 Excel 表格，你知道那是一个矩阵，行是样本、列是特征，转置操作改变的是看数据的视角。

当你看到一篇机器学习论文里的公式 $\mathbf{y} = \mathbf{X}\mathbf{w}$，你能立刻说出：$\mathbf{X}$ 是数据矩阵，$\mathbf{w}$ 是模型参数，$\mathbf{y}$ 是预测结果，整个公式就是一个线性模型。

当你的代码跑出来一个奇异矩阵报错，你知道这不是代码写错了，而是数据本身有问题（特征冗余或样本不足），需要先处理数据再建模。

---

### 1.7.1 本章知识地图

线性代数是数据科学的通用语言。本章建立了从基本概念到数值实现的完整知识框架：

```
向量空间（1.1）
  ├── 向量空间公理、线性相关性、基与维数
  ├── 内积与正交、范数与距离
  └── IVector 接口：创建、运算、切片、统计
       │
       ▼
矩阵运算（1.2）
  ├── 行主序存储、索引与切片
  ├── 矩阵乘法、转置、广播
  ├── 行列式、秩、可逆性
  └── IMatrix 接口：完整矩阵操作
       │
       ▼
矩阵分解（1.3）
  ├── LU（多右端求解）、Cholesky（SPD 系统）
  ├── QR（稳定最小二乘）
  ├── 特征分解（谱分析、PCA）
  ├── SVD（最优低秩逼近、伪逆）
  └── Decomps 工厂：统一入口
       │
       ▼
方程组与最小二乘（1.4）
  ├── solve（方阵）vs lstsq（任意形状）
  ├── 伪逆、投影矩阵
  └── LinearSystemSolver / LeastSquaresSolver
       │
       ▼
应用（1.5）
  ├── PCA（降维）
  ├── 线性回归（最小二乘）
  └── 图像处理、信号滤波
       │
       ▼
数值稳定性（1.6）
  ├── 条件数、避免求逆
  ├── 分解选型、灾难性抵消
  └── SIMD 加速、浮点比较
```

### 1.7.2 核心定理回顾

| 定理 | 内容 | 应用 |
|------|------|------|
| **Cauchy-Schwarz** | $|\langle \mathbf{u},\mathbf{v}\rangle| \leq \|\mathbf{u}\|\|\mathbf{v}\|$ | 余弦相似度、收敛性证明 |
| **秩-零化度** | $\mathrm{rank}(\mathbf{A}) + \dim\mathcal{N}(\mathbf{A}) = n$ | 判断方程组解的情况 |
| **谱定理** | 实对称矩阵有正交特征向量基 | PCA、协方差矩阵对角化 |
| **Eckart-Young** | SVD 截断给出最优低秩逼近 | 推荐系统、降噪 |
| **投影定理** | $\hat{\mathbf{y}} = \mathbf{P}\mathbf{y}$ 是列空间上最近点 | 最小二乘几何解释 |

### 1.7.3 API 速查

| 任务 | API | 备注 |
|------|-----|------|
| 创建向量 | `Linalg.vector(double[])` | 支持 range、ones、zeros、rand |
| 创建矩阵 | `Linalg.matrix(double[][])` | 支持 eye、ones、zeros、rand |
| 矩阵乘法 | `A.mmul(B)` | 区别于 `A.multiply(B)`（逐元素） |
| 矩阵乘向量 | `A.mmul(x)` | |
| 转置 | `A.transpose()` 或 `A.t()` | |
| 特征分解 | `A.eigen()` | 返回 `Tuple2<特征值, 特征向量>` |
| SVD | `A.svd()` | 返回 `Tuple3<U, S, V^T>` |
| QR 分解 | `A.qr()` | 返回 `Tuple2<Q, R>` |
| LU 分解 | `A.lu()` | 返回 `Tuple2<L, U>` |
| Cholesky | `A.cholesky()` | 返回下三角 $\mathbf{L}$ |
| 解方程（方阵） | `A.solve(b)` | 内部自动选分解 |
| 最小二乘 | `A.lstsq(b)` | 返回 `Tuple2<解, 残差>` |
| 伪逆 | `A.pinv()` | |
| 条件数 | `A.cond()` | |
| 中心化 | `data.center()` | 减列均值 |
| 协方差 | `data.covarianceFromCentered()` | |
| 水平拼接 | `A.hstack(B)` | |
| 垂直拼接 | `A.vstack(B)` | |

### 1.7.4 常见误区汇总

1. **混淆 `multiply` 与 `mmul`**：前者逐元素，后者矩阵乘
2. **用 `solve` 解超定系统**：会抛异常，应使用 `lstsq`
3. **显式求逆解方程**：用 `solve` 或 `lstsq`
4. **PCA 前不中心化**：第一主成分被均值主导
5. **忽略条件数**：病态问题应使用正则化或 SVD
6. **SVD 第三分量误为 V**：实际是 $\mathbf{V}^\top$
7. **浮点数用 `==` 比较**：应使用相对容差
8. **忘记截距列**：线性回归需添加全 1 列

### 1.7.5 自测清单

- [ ] 能否独立写出 $\ell_1$、$\ell_2$、$\ell_\infty$ 范数定义并解释几何意义？
- [ ] 能否说明矩阵乘法为何一般不满足交换律？
- [ ] 能否对比特征分解与 SVD 的适用条件？
- [ ] 能否解释投影矩阵在列空间上的作用？
- [ ] 能否用条件数语言解释岭回归缓解共线性的机制？
- [ ] 能否手写 QR 解最小二乘的代数步骤？
- [ ] 能否在 IDE 中独立运行本章所有代码示例？

### 1.7.6 与后续章节的接口

| 后续章节 | 依赖的本章知识 |
|----------|---------------|
| **第 4 章 统计学** | 协方差矩阵、样本均值向量、多元正态的矩阵记号 |
| **第 5 章 机器学习** | 设计矩阵、正规方程、PCA 与 SVD、正则化的谱解释 |
| **第 6 章 最优化** | Hessian 矩阵（对称正定与凸性）、牛顿方向 |
| **第 7 章 信号处理** | 卷积的矩阵形式、Toeplitz 结构、频域对角化 |

### 1.7.7 编程练习

1. 实现一个完整的 PCA 流水线：读数据 → 中心化 → 协方差 → 特征分解 → 投影 → 方差解释率
2. 比较正规方程、QR、SVD 三种方法解同一病态最小二乘问题的精度差异
3. 构造 Hilbert 矩阵族 $H_n$，观察 $n$ 增大时条件数的增长趋势
4. 实现 SVD 低秩图像压缩，比较不同 $k$ 下的压缩比与视觉质量

### 1.7.8 延伸阅读

- 经典教材：Golub & Van Loan《Matrix Computations》（第 4 版）
- 入门友好：Strang《Introduction to Linear Algebra》
- 数值分析：Trefethen & Bau《Numerical Linear Algebra》
- YiShape-Math 文档：[`docs/Matrix-Operations.md`](../../docs/Matrix-Operations.md)、[`docs/Vector-Operations.md`](../../docs/Vector-Operations.md)

### 1.7.9 结语

线性代数是可计算数学的骨架。掌握本章后，你应能：
- 自信地阅读使用矩阵记号的论文
- 在 YiShape-Math 中将公式落地为代码
- 判断何时需要正则化或更稳定的算法

下一章将进入**随机性**的世界：矩阵成为随机对象的实现，向量成为样本。线性代数的确定性语言将与概率论交汇，进入统计推断的主战场。

---

**回到本章开头**：[1.1.Vector.md](1.1.Vector.md)
